{
  "name": "skillswap-connect",
  "version": "1.0.0",
  "description": "SkillSwap Connect - A full-stack MERN + React Native platform for skill exchange",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix mobile",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\" \"npm --prefix mobile run start\" -n backend,frontend,mobile",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:mobile": "npm --prefix mobile run start",
    "docker:up": "docker-compose up --build",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "build": "npm --prefix frontend run build",
    "start:backend": "npm --prefix backend start",
    "start:frontend": "npm --prefix frontend preview",
    "test": "npm --prefix backend run test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
